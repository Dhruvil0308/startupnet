/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,  // Helps catch errors and ensures React hot-reload works smoothly
    swcMinify: true,        // Ensures faster builds and better performance (optional)
  };
  
  export default nextConfig;
  